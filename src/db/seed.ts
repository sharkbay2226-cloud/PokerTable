import { getAllRooms, getAllSessions, getAllTournaments, getAllBankrollEntries } from './db';

const SEED_DONE_KEY = 'poker-diary-seed-done';

export async function seedDatabase() {
  // seed disabled — app starts empty
}
